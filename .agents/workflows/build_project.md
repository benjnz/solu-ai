---
description: Build the complete project
---
Run the following commands to install dependencies and build the Vite React application:

1. Install dependencies:
// turbo-all
```bash
npm install
```

2. Build the project:
```bash
npm run build
```
